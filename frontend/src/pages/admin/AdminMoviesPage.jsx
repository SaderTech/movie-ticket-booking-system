import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { Edit3, Film, Play, Plus, Save, Square, Trash2, X } from 'lucide-react'
import { useState } from 'react'
import toast from 'react-hot-toast'
import { movieApi } from '../../api/movieApi'
import { EmptyState, ErrorState, SkeletonGrid } from '../../components/common/AsyncState'
import { SafeImage } from '../../components/common/SafeImage'
import { StatusBadge } from '../../components/common/StatusBadge'
import { getApiError } from '../../utils/apiError'
import { formatDate, toNumber } from '../../utils/formatters'

const emptyMovie = { title: '', description: '', durationMinutes: '', trailerUrl: '', posterUrl: '', releaseDate: '', ageRating: 'P', status: 'COMING_SOON', genreIds: [], directorIds: [] }

export function AdminMoviesPage() {
  const queryClient = useQueryClient()
  const [editing, setEditing] = useState(null)
  const [form, setForm] = useState(emptyMovie)
  const [actorAssignments, setActorAssignments] = useState([])
  const [actorId, setActorId] = useState('')
  const [roleName, setRoleName] = useState('')
  const movies = useQuery({ queryKey: ['admin', 'movies'], queryFn: () => movieApi.list() })
  const genres = useQuery({ queryKey: ['admin', 'genres'], queryFn: movieApi.genres.list, staleTime: 120000 })
  const actors = useQuery({ queryKey: ['admin', 'actors'], queryFn: movieApi.actors.list, staleTime: 120000 })
  const directors = useQuery({ queryKey: ['admin', 'directors'], queryFn: movieApi.directors.list, staleTime: 120000 })
  const reset = () => { setEditing(null); setForm(emptyMovie); setActorAssignments([]); setActorId(''); setRoleName('') }
  const save = useMutation({ mutationFn: (payload) => editing ? movieApi.update(editing.id, payload) : movieApi.create(payload), onSuccess: () => { toast.success(editing ? 'Đã cập nhật phim' : 'Đã tạo phim'); queryClient.invalidateQueries({ queryKey: ['admin', 'movies'] }); reset() }, onError: (error) => toast.error(getApiError(error)) })
  const lifecycle = useMutation({ mutationFn: ({ id, action }) => action === 'start' ? movieApi.start(id) : movieApi.end(id), onSuccess: () => { toast.success('Đã cập nhật trạng thái phim'); queryClient.invalidateQueries({ queryKey: ['admin', 'movies'] }) }, onError: (error) => toast.error(getApiError(error)) })
  const edit = (movie) => { setEditing(movie); setForm({ title: movie.title || '', description: movie.description || '', durationMinutes: movie.durationMinutes || '', trailerUrl: movie.trailerUrl || '', posterUrl: movie.posterUrl || '', releaseDate: movie.releaseDate || '', ageRating: movie.ageRating || 'P', status: movie.status || 'COMING_SOON', genreIds: (movie.genres || []).map((item) => String(item.id)), directorIds: (movie.directors || []).map((item) => String(item.id)) }); setActorAssignments((movie.actors || []).map((item) => ({ actorId: String(item.id), roleName: item.roleName || '' }))); window.scrollTo({ top: 0, behavior: 'smooth' }) }
  const addActor = () => { if (!actorId || !roleName.trim()) return toast.error('Chọn diễn viên và nhập vai diễn'); if (actorAssignments.some((item) => item.actorId === actorId)) return toast.error('Diễn viên đã được thêm'); setActorAssignments([...actorAssignments, { actorId, roleName: roleName.trim() }]); setActorId(''); setRoleName('') }
  const submit = (event) => { event.preventDefault(); if (!form.genreIds.length || !form.directorIds.length || !actorAssignments.length) return toast.error('Phim cần ít nhất một thể loại, diễn viên và đạo diễn'); save.mutate({ ...form, posterUrl: form.posterUrl || null, durationMinutes: toNumber(form.durationMinutes), genreIds: form.genreIds.map(Number), directorIds: form.directorIds.map(Number), actors: actorAssignments.map((item) => ({ actorId: Number(item.actorId), roleName: item.roleName })) }) }
  const loadingRefs = genres.isLoading || actors.isLoading || directors.isLoading
  return <div><div className="admin-page-title"><div><span className="eyebrow"><Film /> Nội dung điện ảnh</span><h1>Quản lý phim</h1><p>Tạo, cập nhật và chuyển vòng đời phim. Backend không có API xóa phim.</p></div><button className="button button-secondary" onClick={reset}><Plus /> Phim mới</button></div><form className="panel admin-movie-form" onSubmit={submit}><div className="section-heading"><h2>{editing ? `Chỉnh sửa “${editing.title}”` : 'Thêm phim mới'}</h2>{editing && <button className="icon-button" type="button" onClick={reset} aria-label="Hủy chỉnh sửa"><X /></button>}</div><div className="movie-form-layout"><div className="form-grid two"><label>Tên phim<input required maxLength="255" value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} /></label><label>Thời lượng (phút)<input required type="number" min="1" max="600" value={form.durationMinutes} onChange={(e) => setForm({ ...form, durationMinutes: e.target.value })} /></label><label>Ngày khởi chiếu<input required type="date" value={form.releaseDate} onChange={(e) => setForm({ ...form, releaseDate: e.target.value })} /></label><label>Độ tuổi<select value={form.ageRating} onChange={(e) => setForm({ ...form, ageRating: e.target.value })}>{['P', 'C13', 'C16', 'C18'].map((item) => <option key={item}>{item}</option>)}</select></label><label>Trạng thái<select value={form.status} onChange={(e) => setForm({ ...form, status: e.target.value })}>{['COMING_SOON', 'NOW_SHOWING', 'ENDED'].map((item) => <option key={item}>{item}</option>)}</select></label><label>URL trailer<input required type="url" maxLength="500" value={form.trailerUrl} onChange={(e) => setForm({ ...form, trailerUrl: e.target.value })} /></label><label className="full-span">URL poster<input type="url" maxLength="500" value={form.posterUrl} onChange={(e) => setForm({ ...form, posterUrl: e.target.value })} /></label><label className="full-span">Mô tả<textarea rows="5" maxLength="5000" value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} /></label></div><div className="poster-admin-preview"><SafeImage src={form.posterUrl} alt="Xem trước poster" /></div></div><div className="form-grid two reference-fields"><label>Thể loại<select multiple required value={form.genreIds} onChange={(e) => setForm({ ...form, genreIds: Array.from(e.target.selectedOptions, (option) => option.value) })}>{(genres.data || []).map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><small>Giữ Ctrl để chọn nhiều.</small></label><label>Đạo diễn<select multiple required value={form.directorIds} onChange={(e) => setForm({ ...form, directorIds: Array.from(e.target.selectedOptions, (option) => option.value) })}>{(directors.data || []).map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><small>Giữ Ctrl để chọn nhiều.</small></label></div><fieldset className="actor-builder"><legend>Diễn viên và vai diễn</legend><div className="actor-add"><select value={actorId} onChange={(e) => setActorId(e.target.value)}><option value="">Chọn diễn viên</option>{(actors.data || []).filter((actor) => !actorAssignments.some((item) => item.actorId === String(actor.id))).map((actor) => <option value={actor.id} key={actor.id}>{actor.name}</option>)}</select><input value={roleName} onChange={(e) => setRoleName(e.target.value)} placeholder="Tên vai diễn" /><button type="button" className="button button-secondary" onClick={addActor}><Plus /> Thêm</button></div><div className="actor-tags">{actorAssignments.map((item) => { const actor = (actors.data || []).find((entry) => String(entry.id) === item.actorId); return <span key={item.actorId}>{actor?.name || `#${item.actorId}`} — {item.roleName}<button type="button" onClick={() => setActorAssignments(actorAssignments.filter((entry) => entry.actorId !== item.actorId))} aria-label="Bỏ diễn viên"><X /></button></span> })}</div></fieldset><button className="button button-primary" disabled={save.isPending || loadingRefs}><Save /> {save.isPending ? 'Đang lưu...' : editing ? 'Cập nhật phim' : 'Tạo phim'}</button></form><section className="panel table-panel admin-list-section"><div className="section-heading"><h2>Danh sách phim</h2><span className="badge neutral">{movies.data?.length || 0} phim</span></div>{movies.isLoading ? <SkeletonGrid count={5} compact /> : movies.isError ? <ErrorState message={getApiError(movies.error)} onRetry={movies.refetch} /> : !(movies.data || []).length ? <EmptyState title="Chưa có phim" /> : <div className="table-scroll"><table><thead><tr><th>Poster</th><th>Phim</th><th>Ngày chiếu</th><th>Trạng thái</th><th>Thao tác</th></tr></thead><tbody>{movies.data.map((movie) => <tr key={movie.id}><td><SafeImage className="table-poster" src={movie.posterUrl} alt={movie.title} /></td><td><strong>{movie.title}</strong><small>{movie.durationMinutes} phút · {movie.ageRating}</small></td><td>{formatDate(movie.releaseDate)}</td><td><StatusBadge value={movie.status} /></td><td><div className="row-actions"><button className="button button-secondary button-sm" onClick={() => edit(movie)}><Edit3 /> Sửa</button>{movie.status === 'COMING_SOON' && <button className="button button-success button-sm" onClick={() => lifecycle.mutate({ id: movie.id, action: 'start' })}><Play /> Bắt đầu</button>}{movie.status === 'NOW_SHOWING' && <button className="button button-warning button-sm" onClick={() => lifecycle.mutate({ id: movie.id, action: 'end' })}><Square /> Kết thúc</button>}</div></td></tr>)}</tbody></table></div>}</section></div>
}
